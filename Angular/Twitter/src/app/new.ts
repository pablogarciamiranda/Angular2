import { User } from './user';
export class New {

    constructor(public id: number, public user:string, public title: string, public comments: string, public points: number, public url: string ){

    }
}
